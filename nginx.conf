
server {
  # Define listening port and server name
  listen 8080;
  server_name localhost;

  # Root directory for your HTML files
  root /var/www/html;

  # Index files to serve
  index index.html index.htm;

  # Location blocks for specific resources (optional)
  # ...

}


